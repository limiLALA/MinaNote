# 2024H2
## 自我介绍

我叫XXX，2021年7月从厦门大学毕业，7月底入职腾讯，成为支付平台测试开发，曾参与专项检测平台和现网引流平台方案设计与开发、结算业务版本测试，先后担任资金账务测试SE、支付容灾平台测试负责人.

在这几年工作经历里边，让我印象最深刻的项目是最近的这个Groot容灾切换平台。该平台作为基建，为所有支付金融业务提供前后端支持高可用、快速的容灾切换能力，其中最复杂、风险最高的模块是C++SDK，要集成到所有支付金融的业务服务当中，适配多种不同的开发框架和不同类型的环境，且API调用耗时要求在100微秒以内。该SDK由我独立负责测试，同时作为项目的负责人，把控项目整体进展，梳理全局风险，主导后端关键链路的大数据量测试、性能压测、稳定性测试。我认为这个项目对我的能力提升有很大的帮助。

细节：

1. 我前期针对最复杂、风险最高的C++SDK开展了全方位的专项测试，设计用例达400+，包含功能、边界、异常、多进程/线程并发、死锁、SHM专项、性能、稳定性、内存泄漏等测试。碰到了一些技术问题：

   1. 并发死锁：死锁是多个进程争夺系统资源而产生的相互等待的现象。在GrootSDK中，1个共享资源就是SHM，多进程并发写SHM时，必须加锁，写完数据再解锁，其他进程再来抢锁，否则数据会写乱。设计原则是锁粒度要足够小，避免锁定时间太长，造成大量进程阻塞。随之带来的就是死锁问题，如解锁前线程挂掉，这个锁就永远不会释放，导致阻塞线程会越来越多，造成功能不可用。解决方案有2种：（1）使用时间锁，给锁设置超时时间，超过时间未解锁，则由其他进程强制解锁。（2）给锁加上归属的进程pid，其他进程如果检测到pid不在了，则强制解锁。
   2. 死锁的4个必要条件：互斥、占有且等待、不可抢占、循环等待。其实在这个场景中，不存在占有且等待和循环等待，而是加锁后进程意外挂掉了（业务重启时触发）从而满足了这2个必要条件，此时SHM已经记录了之前的锁信息，不会被释放。解决方案就是破坏1个死锁必要条件：不可抢占。给锁带上超时时间或者归属pid，在满足条件时可以被其他进程抢占，从而实现强解锁。
   3. 复现死锁：使用gdb在加锁后断点，kill进程，在SHM内留下加锁信息。然后重启服务，此时会检测到SHM有锁，从而进入全局阻塞状态。
   4. 性能问题：SDK的异步定时器收到一条记录中包含了1个长列表，没有处理直接写缓存。实时API调用会去遍历这个长列表，时间复杂度O(n)，耗时达到500us以上，业务无法接受。解决是提前构建索引map，API直接通过map哈希，时间复杂度降低到O(1)，耗时降至40us。

2. 大数据量测试：春节后，开发发现春节扩容导致Groot平台压力倍增，存在资源风险，给我们测试提了1个模糊需求，验证在客户端扩容+Groot后端单集群故障后，后端服务能否承载巨大的RPS和数据量。要求提供具体量级时，也只是拍脑袋估了个大概。前负责人没有进行详细的需求分析、设计具体执行方案，就直接按大场景分工开干了。要先写数据构造脚本、压测脚本，每轮压测都要经历清空etcd、重置后端和SDK桩、准备数据和压测配置、发压，过程中持续观察服务的异常表现，压完还要收集进程资源，分析毛刺等问题。记录压测报告数据，和开发勾兑发现的问题，每次开发都会说这个数据有问题，要修正，然后就修一点点，再压测，循环往复持续了好几周，感觉一直在做无用功，但是开发又给不出具体的不同业务场景的数据量和模型。前负责人走后，我开始介入，察觉到开发没有精力和方法去帮我们收集现网真实的数据，给出的估算数据非常不靠谱，否则也不会一直返工。我便开始重新梳理需求，将笼统的大问题拆分成开发可以明确给出答案的小问题，从七八十个业务场景中选出七八个典型场景，再申请现网权限，和接入Groot的不同业务方负责人直接沟通，记录他们的使用情况，再从现网查询详细的客户端、usage、value数量，整理成表，再和开发确认这些数据是否符合他们预期，然后再针对不同业务场景分类计算最终用于压测的数据量。并让开发配合查询现网机型配置，我重新申请相同配置机器，搭建更接近现网的压测环境。最后按现网数据量、春节扩容量、扩容+单集群故障量3种大场景去压测，比对资源占用情况，分析不达标的原因，和开发共同思考优化方案。pb数据结构设计不合理，存在大量冗余数据，浪费网络传输、序列化、反序列的资源占用和耗时，给出优化后的pb结构，开发再进一步评估细化，最终将资源占用降低了50%

3. 混沌工程演习：从现网部署架构出发，设计了1套混沌演习测试环境，基本保留关键容灾特性，验证Groot平台自身的高可用，降低硬件扰动影响，间接发现一些系统依赖关系和设计问题。

   1. 混沌故障大类：(1)外部依赖故障；(2)基础设施故障；(3)网络故障；(4)状态异常；(5)单点故障；(6)单机房故障；(7)单集群故障

   2. 稳态评估指标：吞吐量、耗时、失败量、RTO

   3. 演习失败原因分析：故障恢复时间过长、服务不可自恢复；检查算法太严格，存在无效监控指标；监控平台采集数据不稳定

   4. 发现了Groot后端大部分服务都存在故障熔断效率极低的问题。预期下游实例故障5~10s，上游就要将这个实例熔断掉，短期内都不会再访问这个故障实例，这样整体功能才不受影响。但实际在混沌演习过程中，故障2分钟内，上游一直有流量过来，然后不断报错告警，大量正常功能不可用。最后发现是没有配置熔断策略，使用了北极星默认的熔断策略：（1）60S内连续10次失败，或者60S内失败率>50%，才会发起熔断；（2）即使熔断了，30s就会半开，此时服务尚未恢复，上游又会出现调用失败量。影响：降低系统可用率，业务切换不及时，影响到接入业务自身的可用性。

   5. 解决：修改熔断配置

      （1）降低熔断门槛，连续失败10次改为2次就熔断

      （2）熔断后，30s半开改为300s半开，给下游服务充足的恢复时间，避免反复熔断

   6. 线程挂起对golang不起作用：C++依赖OS调度器，属于内核级线程模型，用户态线程:内核态线程=1:1，线程的创建与销毁都需要CPU参与，成本大

21下半年参与了专项检测平台二期建设，负责设计各个工具的接入方案、蓝盾插件的方案，分别使用Vue和Python开发了前后端。

22年主导了现网引流项目，针对结算和资金账务的批跑类业务设计了一整套通用的数据采集、解析、脱敏、重放的引流方案，打通线上到IDC的专项环境，拉通业务开发、DBA确定脱敏方案，最终将动态的现网数据不断沉淀为静态测试用例，嵌入FCD流程，设置质量红线，帮助开发把控版本质量。期间穿插了一些结算、资金账务业务版本测试、自动化用例建设、资金安全用例设计与实现、环境维护与值班的工作。

## 工作项目经验

### 大数据量测试

在开发团队提供的数据模糊不清的情况下，敏锐察觉团队低效状态，及时叫停无效压测，重新收集精确的现网数据，与开发团队多次沟通确认最终数据量和机型配置，重建了与现网相似的配置环境，基于新数据重新压测，终于达到预期效果，推动开发团队解决了性能缺陷

在构建大数据量压力测试的过程中，发现etcd的DbSize增长过快且频繁超时。通过分析后端写入的数据结构，识别出大量冗余数据以及不合理的使用方式，导致平台整体性能未达预期。向开发团队提供了优化后的数据结构和正确的使用方法，推动了PB协议的改造，显著提升了大数据量场景下全链路的性能，减轻了etcd的数据压力，整体效率提升了130%

### 性能测试与优化

针对CppSDK/Go服务模块，根据实际需求设计最合理的数据量、并发度及机器资源配置，以测试并挖掘系统性能的边界。同时，依据现网的实际流量规模，构建定制化RPS压测模型。凭借丰富经验，迅速发现并准确定位接口响应时间过长、异常波动（毛刺）、资源利用异常等关键问题，为开发团队提供明确的优化方向，有效规避因性能问题可能导致的支付业务经济损失

### 自动化用例用例回归提效

**Situation（情境）**:
在Go语言开发环境中，面临一个挑战：Go语言当时缺乏与Python的pytest相媲美的成熟开源测试框架。初期采用的testify/suite虽然功能强大，但不支持套件内用例的并行执行。

**Task（任务）**:
随着项目的发展，测试用例数量急剧增加，导致执行时间远远超出了预设的10分钟限制。这不仅影响了回归测试的效率，还阻碍了持续集成和持续部署（CI/CD）流程的顺畅进行。

**Action（行动）**:
为了解决这个问题，首先尝试对现有源码进行改造以支持并行执行。然而很快遇到了技术上的瓶颈，推测这可能是开源社区未广泛支持并行的原因之一。随后设计了一个临时解决方案：将单个测试文件中的单一测试套件拆分为多个文件中的多个测试套件，以提高并发度。尽管这个方法在一定程度上缓解了超时问题，但它仍然无法在一个进程中完成一个包内所有用例的执行，因此无法将其完全集成到CI/CD流水线中。

为了彻底解决问题，我决定重新设计一套全新的测试框架。这次，我放弃了所有现有的开源测试框架，转而利用Go语言的单例模式、WaitGroup和Mutex等特性，实现了一种既能共享资源又能全并发加速执行的框架。

**Result（结果）**:
通过这一创新性的改进，go test pkg的执行时间从原先的超过10分钟大幅缩短至仅需5分钟。更重要的是，我将测试用例成功集成到了CI/CD流水线中，极大地避免了免测版本的测试人力浪费，并显著降低了提测版本的回归成本。

### 现网引流任务耗时优化

**Situation（情境）**:
在线上引流平台中，遇到了采集解析耗时过长的问题，严重影响了系统的性能和响应速度。

**Task（任务）**:
为了解决这个问题，需要对系统进行分段排查，找出性能瓶颈并进行优化。

**Action（行动）**:
经过排查，定位到单次跨机房调用时间长达3秒，且在单任务内频繁调用。由于查询的数据均为变动极少的静态数据，多次查询意义不大。因此，重新设计了一个子方案：将其他机房的数据预先缓存在本地存储库中。

**Result（结果）**:
通过这一优化措施，实时链路现在只需查询本系统内的Elasticsearch以获取静态数据，无需再跨机房调用微服务。单次查询耗时从秒级降至毫秒级，整体查询耗期从1800秒大幅缩减至20秒，显著提升了系统的性能和效率。

### 混沌工程演练

#### etcd故障分析

**Situation（情境）**:
在混沌工程演练期间，etcd频繁发生故障，影响了系统的稳定性。

**Task（任务）**:
需要深入分析etcd的故障原因，并提出有效的解决方案。

**Action（行动）**:
通过分析日志、监控数据和服务异常，我们深入研究了etcd的工作原理，并尝试进行调优。揭示了etcd低版本的三个缺陷及其与后端服务缺陷的交互影响，推动开发团队升级etcd版本并修复相关服务缺陷。

**Result（结果）**:
有效预防了潜在的线上事故，提高了系统的整体稳定性。

#### Go服务线程挂起故障失效分析

**Situation（情境）**:
对golang服务中注入单个定时任务线程挂起故障，但是导致服务进程全部卡住。

**Task（任务）**:
找出表现不符合预期的根源并提出解决方案。

**Action（行动）**:
对混沌故障注入工具实现机制和golang调度器原理进行了深入探究，特别关注ptrace软中断在golang中的有效性。

**Result（结果）**:
得出结论，ptrace软中断对golang无效，并推动混沌平台调整了线程挂起故障的适用范围，从而减少了对用户的误导并提高了故障诊断的准确性。

### 专项检测平台优化

#### 简历版

**初版问题**：首代方案过于强调通用性，接入工具形式繁多（包括脚本、微服务、插件等）导致执行难。

**转变策略**：二代方案注重实用性与扩展性，通过与接入方紧密合作，统一接口规范，以微服务形式接入和调用，降低维护成本。

**其他**：全面优化质量风险展示方案，新增风险处理、审核及用户反馈机制，确保风险及时应对，用户意见得到重视，从而显著提升了平台的整体效能和用户满意度。

#### STAR法则版

##### 接入方案优化

**Situation (情境)**:初期，我们的平台设计过分追求通用性，以适应多样化的工具接入，包括脚本、微服务、插件等。

**Task (任务)**:随着实施过程中的困难逐渐显现，我们需要转变设计思路，提高平台的实用性和可维护性。

**Action (行动)**:在第二代方案中，我们强化了与工具接入方的沟通协作，统一了接口规范。所有工具都以微服务形式接入，平台也提供相应的微服务供调用。

**Result (结果)**:这样的转变不仅增强了平台的可用性和扩展性，还显著降低了维护成本。此外，我们还全面优化了质量风险展示方案，加入了风险处理、审核和用户反馈机制，进一步提升了平台的效能和用户满意度。

##### 质量风险展示优化

**Situation (情境)**:原有的质量风险展示方案存在不足，缺乏完善的风险处理和用户反馈机制。

**Task (任务)**:为了提升平台的风险管理能力和用户体验，我们需要对质量风险展示方案进行全面优化。

**Action (行动)**:我们增加了风险处理流程、审核机制以及用户反馈渠道。

**Result (结果)**:通过这些改进，我们的平台现在能够更有效地管理和展示质量风险，同时用户也能更方便地提供反馈，从而形成了一个更加完善和用户友好的风险管理体系。
